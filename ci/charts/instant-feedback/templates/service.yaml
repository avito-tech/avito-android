apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}
  namespace: services
  labels:
    name: {{ .Chart.Name }}
    service: {{ .Chart.Name }}
spec:
  selector:
    app: {{ .Chart.Name }}
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
