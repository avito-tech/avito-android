package com.avito.report.internal.model

/**
 * Прилетает в prepared_data после анализа совпадения с тестохранилкой если есть test_case_id
 */
internal data class KtulhuTestCase(val description: String?)
